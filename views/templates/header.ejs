<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="shortcut icon" type="image/png" href="/logo.png">
  <title>ProtectUs</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet"
    href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
  <link rel="stylesheet"
    href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
  <link rel="stylesheet" href="./styles/navbar.css">
  <style>
    body {
      overflow-x: hidden;
      /* Prevent horizontal scrolling when sidebar is open */
    }

    #navbar {
      width: 100%;
      position: fixed;
      z-index: 20;
      /* Ensure the navbar is above the overlay */
      top: 0;
      background: rgba(81, 95, 132, 0.9);
      backdrop-filter: blur(10px);
    }

    #overlay {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.5);
      backdrop-filter: blur(5px);
      z-index: 15;
      display: none;
      /* Initially hidden */
    }

    #overlay.show {
      display: block;
    }

    #sidebar {
      top: 72px;
      right: -75%;
      width: 75%;
      height: calc(100% - 72px);
      transition: right 0.3s ease-in-out;
      position: fixed;
      z-index: 20;
      background: #3e4c6e;
      color: white;
      border-top-left-radius: 20px;
      border-bottom-left-radius: 20px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
      /* Add shadow here */
    }

    #sidebar.show {
      right: 0;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
      /* Add shadow here */
    }

    .menu-btn {
      position: relative;
      width: 30px;
      height: 30px;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      cursor: pointer;
    }

    .menu-btn span {
      height: 3px;
      width: 100%;
      background: white;
      border-radius: 5px;
      transition: transform 0.3s, opacity 0.3s;
    }

    .menu-btn.open span:nth-child(1) {
      transform: rotate(45deg) translateY(19.2px);
    }

    .menu-btn.open span:nth-child(2) {
      opacity: 0;
    }

    .menu-btn.open span:nth-child(3) {
      transform: rotate(-45deg) translateY(-19.2px);
    }

    .sidebar ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .sidebar ul li {
      margin: 1rem 0;
    }

    .sidebar ul li a {
      text-decoration: none;
      color: white;
      font-size: 1.25rem;
      transition: color 0.3s, background 0.3s;
      display: block;
      padding: 0.75rem 1rem;
      border-radius: 10px;
    }

    .sidebar ul li a:hover {
      background: #515f84;
      color: #EAD797;
    }

    .sidebar ul li ul {
      margin-top: 0.5rem;
      padding-left: 1.5rem;
    }

    .sidebar ul li ul li a {
      font-size: 1rem;
      padding: 0.5rem 1rem;
    }

    .sidebar ul li ul li a:hover {
      background: #636e8b;
    }

    .logo-container {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin-bottom: 2rem;
    }

    .logo-container img {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      border: 3px solid #515f84;
    }

    .logo-container h1 {
      font-size: 2rem;
      font-weight: bold;
      margin-top: 1rem;
      color: #EAD797;
    }
  </style>
</head>

<body class="bg-[#515F84]">
  <div class="bg-[#515F84] py-4 px-6 flex justify-between items-center" id="navbar">
    <input type="text"
      class="w-3/4 md:w-2/4 px-4 py-2 rounded-lg bg-[#c1c4cd] text-white focus:outline-none focus:bg-gray-900"
      placeholder="Search...">
    <div class="menu-btn cursor-pointer" id="hamburgerMenu">
      <span></span>
      <span></span>
      <span></span>
    </div>
  </div>

  <div id="overlay"></div>

  <div id="sidebar" class="sidebar py-8 px-6 h-full overflow-y-auto">
    <div class="logo-container">
      <img src="logo.png" alt="Logo">
      <h1>ProtectUs</h1>
    </div>
    <ul class="text-left">
      <li><a href="/profile">Profile</a></li>
      <li>
        <a href="#">Criminals near you</a>
        <ul>
          <li><a href="/map">Map</a></li>
          <li><a href="/list">List</a></li>
        </ul>
      </li>
      <li><a href="/protection">Protection Services</a></li>
      <li><a href="/about.html">Contact Us</a></li>
      <li class="mt-8"><a href="/">Log Out</a></li>
    </ul>
  </div>

  <script>
    document.getElementById('hamburgerMenu').addEventListener('click', () => {
      const sidebar = document.getElementById('sidebar');
      const overlay = document.getElementById('overlay');
      const menuIcon = document.getElementById('hamburgerMenu');
      sidebar.classList.toggle('show');
      overlay.classList.toggle('show');
      menuIcon.classList.toggle('open');

      // Disable scrolling when sidebar is open
      if (sidebar.classList.contains('show')) {
        document.body.style.overflowY = 'hidden';
      } else {
        document.body.style.overflowY = 'auto';
      }
    });

    // Close the sidebar when clicking outside of it
    document.getElementById('overlay').addEventListener('click', () => {
      const sidebar = document.getElementById('sidebar');
      const overlay = document.getElementById('overlay');
      const menuIcon = document.getElementById('hamburgerMenu');
      sidebar.classList.remove('show');
      overlay.classList.remove('show');
      menuIcon.classList.remove('open');
      document.body.style.overflowY = 'auto'; // Re-enable scrolling
    });
  </script>
</body>

</html>