<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ProtectUs</title>
    <link rel="shortcut icon" type="image/png" href="/logo.png" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
</head>

<body class="bg-[#515F84] text-white flex flex-col justify-center items-center h-screen">
    <!-- Back button -->
    <a href="/">
        <span
            class="material-symbols-outlined absolute top-0 left-0 mt-4 ml-4 inline-block px-3 py-1 bg-[#515F84] rounded-lg text-[#EAD797]">
            arrow_back_ios
        </span>
    </a>
    <br /><br /><br /><br />
    <img src="logo.png" alt="" class="w-[290px] h-[280px] justify-center" />
    <br />

    <div class="max-w-md mx-auto bg-[#EAD797] rounded-lg p-8 shadow-[0_0px_10px_0px_rgba(0,0,0,0.5)] mb-8">
        <!-- Signup form -->
        <h2 class="text-2xl mb-4 text-[#223053]">Signup</h2>
        <!-- Form to handle user signup, submits data to the /signup endpoint -->
        <form action="/signup" method="post">
            <div class="mb-4">
                <label for="name" class="block mb-2 text-[#223053]">Name</label>
                <!-- Input field for user's name, required for submission -->
                <input type="text" id="name" name="name" class="w-full px-4 py-2 rounded-lg bg-white text-zinc-950"
                    placeholder="Adam Smith" required />
            </div>
            <div class="mb-4">
                <label for="email" class="block mb-2 text-[#223053]">Email</label>
                <!-- Input field for user's email, required for submission -->
                <input type="email" id="email" name="email" class="w-full px-4 py-2 rounded-lg bg-white text-zinc-950"
                    placeholder="email@example.com" required />
            </div>
            <div class="mb-4">
                <label for="password" class="block mb-2 text-[#223053]">Password</label>
                <!-- Input field for user's password, required for submission -->
                <input type="password" id="password" name="password"
                    class="w-full px-4 py-2 rounded-lg bg-white text-zinc-950" placeholder="*****" required />
            </div>
            <!-- Submit button for the signup form -->
            <button type="submit" class="w-full bg-[#515F84] text-white py-2 rounded-lg">
                Signup
            </button>
        </form>
    </div>

    <% if (typeof error !=='undefined' ) { %>
        <!-- Error handling modal, displayed if there's an error during signup -->
        <div id="errorModal" class="fixed flex items-center justify-center w-3/4">
            <div class="bg-white p-8 rounded-lg shadow-lg">
                <p class="text-[#515F84]">
                </p>
                <button onclick="closeModal()" class="mt-4 px-4 py-2 bg-[#515F84] text-white rounded-lg">Close</button>
                <a href="/login" class="mt-4 px-4 py-2 bg-[#515F84] text-white rounded-lg inline-block">Login</a>
            </div>
        </div>
        <% } %>

            <script>
                // Close an error modal and, if an error exists, sets the error modal to display when the document is fully loaded
                /* Generated by ChatGPT 4o
     
                @author https://chat.openai.com/ 
                */
                function closeModal() {
                    document.getElementById('errorModal').style.display = 'none';
                }
        <% if (typeof error !== 'undefined') { %>
                    // Display the error modal when the document is fully loaded if an error exists
                    document.addEventListener('DOMContentLoaded', () => {
                        document.getElementById('errorModal').style.display = 'flex';
                    });
        <% } %>
            </script>
</body>

</html>